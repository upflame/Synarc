module.exports = {
  generateS0p1(w) {
    w('## S0.1 — RUNTIME DETECTION');
    w('');
    w('Detect the execution environment at the start of every session. Runtime detection determines persistence model, output format, available protocols, and injection behavior. Detection runs silently — the user never sees the detection logic, only its effects.');
    w('');
    w('### Detection Signals');
    w('');
    w('| Signal | Runtime | Persistence |');
    w('|--------|---------|-------------|');
    w('| /brain/ or /claude/ exists | Claude Code | Full brain dir + hooks |');
    w('| Filesystem inaccessible, chat only | Claude Web | Conv state blocks |');
    w('| AGENTS.md in repo root | Codex CLI | AGENTS.md protocol |');
    w('| .cursor/rules detected | Cursor IDE | IDE rules protocol |');
    w('| .windsurfrules detected | Windsurf IDE | IDE rules protocol |');
    w('| API call with skill_id | Claude API | Stateless JSON |');
    w('| Copilot env/config detected | Copilot | AGENTS.md protocol |');
    w('| No signal matches | Generic | Claude Code model |');
    w('');
    w('Detection priority: Claude Code > AGENTS.md > Cursor > Windsurf > Claude Web > Copilot > Generic.');
    w('');
    w('### Per-Runtime Output Format');
    w('');
    w('| Runtime | Header | Footer | Box Drawing | Brain Writes |');
    w('|---------|--------|--------|-------------|--------------|');
    w('| Claude Code | Full SYNARC block | Auto-emit | Unicode | Yes |');
    w('| Claude Web | Inline | Compact | Unicode | No (conv blocks) |');
    w('| Codex CLI | [SYNARC] prefix | Auto-emit | ASCII | Via AGENTS.md |');
    w('| Cursor Chat | Inline footer | Auto-emit | Unicode | Limited |');
    w('| Cursor Inline | No header | Compact | Unicode | Limited |');
    w('| Windsurf Chat | Inline footer | Auto-emit | Unicode | Limited |');
    w('| Windsurf Inline | No header | Compact | Unicode | Limited |');
    w('| Claude API | Structured JSON | N/A | N/A | Stateless |');
    w('| Copilot | [SYNARC] prefix | Auto-emit | ASCII | Via AGENTS.md |');
    w('| Generic | Full context block | Auto-emit | Unicode | Conditional |');
    w('');
    w('### Runtime-Specific Protocol Loading');
    w('');
    w('When Claude Web is detected: Load S19 (Claude Web Mode) before proceeding. This section provides conversation state block format, persistence-free operation protocols, and chat-optimized output formatting.');
    w('');
    w('When Codex CLI is detected: Load references/platform-adapters.md section SCODEX. This section provides AGENTS.md format specifications, ASCII-only rendering rules, and limited context handling.');
    w('');
    w('When Cursor or Windsurf is detected: Load references/platform-adapters.md section SIDE. This section provides IDE rules protocol, inline vs chat mode detection, and compact injection formats.');
    w('');
    w('When Claude API is detected: Use structured JSON output format. The classification, risk, and tracking data are returned as structured fields in the API response, not as rendered text. The caller is responsible for persistence.');
    w('');
    w('When Generic runtime is detected: Use the most conservative settings. Full context block, best-effort persistence, Unicode output. If brain directory can be created, initialize it; otherwise, keep all state in conversation memory.');
    w('');
    w('### Detection Resolution for Dual-Signal Environments');
    w('');
    w('Some environments present multiple detection signals. For example, Cursor may have both .cursor/rules and AGENTS.md. The resolution rule: use the more capable persistence model. AGENTS.md > IDE rules > chat-only. Within the same capability tier, prefer the most recently modified signal file.');
    w('');
    w('| Dual Signal | Resolution | Rationale |');
    w('|-------------|------------|----------|');
    w('| AGENTS.md + .cursor/rules | AGENTS.md | Full session persistence > IDE rules |');
    w('| /brain/ + AGENTS.md | /brain/ | Brain directory is the canonical store |');
    w('| .cursor/rules + .windsurfrules | Both — use IDE from signal | They are different IDEs |');
    w('| /brain/ + no AGENTS.md | Claude Code | Brain dir is unique to Claude Code |');
    w('');
    w('### Runtime Capability Matrix');
    w('');
    w('| Capability | Code | Web | Codex | Cursor | Windsurf | API | Copilot |');
    w('|------------|------|-----|-------|--------|----------|-----|--------|');
    w('| Brain dir | Yes | No | No | No | No | No | No |');
    w('| Session blocks | Yes | Yes | No | No | No | No | No |');
    w('| AGENTS.md | No | No | Yes | No | No | No | Yes |');
    w('| IDE rules | No | No | No | Yes | Yes | No | No |');
    w('| Structured JSON | No | No | No | No | No | Yes | No |');
    w('| File operations | Yes | No | Yes | Yes | Yes | No | Yes |');
    w('| Command execution | Yes | No | Yes | No | No | No | Limited |');
    w('| Auto-emit headers | Yes | Yes | Yes | Yes | Yes | N/A | Yes |');
    w('| Checkpoints | Yes | Alt | Alt | Limited | Limited | Caller | Alt |');
    w('| Ledger persistence | Yes | Session | Session | Session | Session | Caller | Session |');
    w('');
    w('### Detection Timing');
    w('');
    w('Detection runs once at session start and can re-run on significant context changes. Re-detection triggers: runtime environment variable changes, new signal files appear, connection type changes (e.g., chat to inline in Cursor), or explicit user reconfiguration.');
    w('');
    w('Re-detection is lightweight — it checks for the presence of signal files and compares against the current detected runtime. If nothing changed, the cached detection is reused. If the runtime changed mid-session, the persistence model adapts and a note is added to the ledger.');
    w('');
    w('### Unknown Runtime Protocol');
    w('');
    w('When runtime cannot be determined from available signals:');
    w('1. Default to Generic Agent — this is the most compatible mode');
    w('2. Attempt to create /brain/ directory — if successful, use brain persistence');
    w('3. If brain creation fails, fall to conversation state blocks');
    w('4. Emit a single detection note: "Runtime auto-detected as Generic. Brain persistence: [enabled/disabled]"');
    w('5. Continue with full feature set minus runtime-specific optimizations');
    w('');
    w('No runtime detection scenario results in degraded functionality. Every feature has a fallback path. The only variance is in persistence model and output formatting — classification, risk assessment, quality gates, and language rules work identically across all runtimes.');
    w('');
  }
};
