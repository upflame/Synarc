module.exports = {
  generateS1(w) {
    w('## S1 — WORK TYPE CLASSIFICATION');
    w('');
    w('Classify every interaction before producing output. Classification is the foundation of risk assessment, quality gates, test requirements, deployment strategy, and session tracking. A misclassified interaction cascades errors through every downstream system.');
    w('');
    w('### Primary WorkTypes');
    w('');
    w('| WorkType | Description | Risk Default | Tracking | Examples |');
    w('|----------|-------------|-------------|----------|----------|');
    w('| FEATURE | New functionality, additive behavior | MEDIUM | Full | "Add X", "build Y", "implement Z" |');
    w('| FIX | Bug, error, crash, regression | HIGH | Full | "broken", "not working", "why", error trace |');
    w('| REFACTOR | Restructure without behavior change | MEDIUM | Full | "clean up", "extract", "reorganize" |');
    w('| SCHEMA | Data model, migration, field change | HIGH | Full | DB migration, model change, event shape |');
    w('| CONTRACT | API boundary, type export, signature | HIGH | Full | Route change, response shape, function sig |');
    w('| CONFIG | Environment, flags, secrets, CI/CD | HIGH | Full | ENV vars, config files, deployment params |');
    w('| INFRA | Infrastructure, deployment, platform | HIGH | Full | Docker, K8s, Terraform, cloud, network |');
    w('| EXPERIMENT | POC, prototype, spike, research | LOW | Full | "try this", "test if", "explore" |');
    w('| DOCS | Documentation, comments, guides | LOW | Full | README, ADRs, comments, diagrams |');
    w('| ANALYSIS | Understanding, explanation, research | INFO | No | "explain", "what does", "how does" |');
    w('| PLAN | Planning, design, architecture | MEDIUM | Full | Roadmap, ADR, feature planning |');
    w('| INCIDENT | Production issue, outage, security | CRITICAL | Full | Pager alert, outage, data problem, CVE |');
    w('');
    w('### Planned vs Unplanned Sub-Classification');
    w('');
    w('Every change is sub-classified as PLANNED or UNPLANNED. PLANNED changes were described by the user before coding began — they exist in a plan, ticket, specification, or prior conversation. UNPLANNED changes are discovered during work — reactive expansions, incidental fixes, opportunistic refactors.');
    w('');
    w('Unplanned changes are not prohibited, but they must be flagged before execution. The UNPLANNED flag ensures the user maintains awareness of scope expansion and can make an informed decision about whether the expansion is worth the additional risk.');
    w('');
    w('**UNPLANNED detection patterns:**');
    w('- Bug fix that requires touching unrelated modules beyond the error source');
    w('- Feature that expands beyond original specification (scope creep)');
    w('- Refactoring discovered during a FIX session');
    w('- Adding error handling to unrelated functions while fixing one');
    w('- Updating documentation beyond what the current change requires');
    w('- Renaming symbols while fixing something else');
    w('- Adding dependencies without pre-declaration');
    w('- Reformatting code while making functional changes');
    w('- Adding tests for unrelated functionality');
    w('- Changing configuration values outside the problem domain');
    w('');
    w('### UNPLANNED Flag Format');
    w('');
    w('When an unplanned change is detected, emit this flag before the expanded execution:');
    w('');
    w('> UNPLANNED CHANGE DETECTED');
    w('> Declared scope: [original task]');
    w('> Actual change: [what expanded]');
    w('> Reason: [why encountered]');
    w('> Additional risk: [level — what new risk this introduces]');
    w('> Recommendation: [proceed with awareness / separate PR / defer]');
    w('');
    w('### FEATURE Sub-Types');
    w('');
    w('| Sub-Type | Risk Default | Description | Examples |');
    w('|----------|-------------|-------------|----------|');
    w('| FEATURE:PLANNED | MEDIUM | Pre-specced, agreed upon | Endpoint from API spec, component from design |');
    w('| FEATURE:UNPLANNED | HIGH | Discovered during work | "While fixing X, we also need Y" |');
    w('| FEATURE:SPIKE | LOW | Research spike | Tech evaluation, feasibility study |');
    w('| FEATURE:FLAG | MEDIUM | Behind feature flag | Ships dark, toggled on later |');
    w('| FEATURE:MVP | MEDIUM | Minimum viable version | First iteration, core path only |');
    w('| FEATURE:ITERATION | MEDIUM | Improvement on existing | V2, enhancement, optimization |');
    w('| FEATURE:MIGRATION | HIGH | Replace system | Data migration, API migration, tech migration |');
    w('');
    w('### FIX Sub-Types');
    w('');
    w('| Sub-Type | Risk Default | Description | Examples |');
    w('|----------|-------------|-------------|----------|');
    w('| FIX:BUG | HIGH | Wrong calculation, incorrect logic | Off-by-one, wrong conditional, wrong type |');
    w('| FIX:CRASH | HIGH | Unhandled exception, OOM | Null pointer, segfault, out of memory |');
    w('| FIX:REGRESSION | HIGH | Previously worked, now broken | Broken by recent deploy, broken by refactor |');
    w('| FIX:SECURITY | CRITICAL | Auth bypass, injection | SQLi, XSS, CSRF, auth bypass, privilege esc |');
    w('| FIX:DATA | CRITICAL | Data corruption, wrong migration | Wrong values in DB, lost data, duplicate records |');
    w('| FIX:PERFORMANCE | MEDIUM | Slow query, memory leak | N+1 query, O(n^2) algo, connection leak |');
    w('| FIX:SILENT | HIGH | Wrong result, no error | Wrong calculation, silent data corruption |');
    w('| FIX:FLAKE | HIGH | Intermittent failure | Race condition, timing bug, network race |');
    w('| FIX:DEPENDENCY | HIGH | CVE, breaking upgrade | Package vuln, deprecation, breaking change |');
    w('| FIX:CONFIG | HIGH | Wrong configuration | Wrong URL, wrong timeout, wrong pool size |');
    w('| FIX:UI | MEDIUM | Visual or UX bug | Wrong layout, broken interaction, a11y issue |');
    w('| FIX:TYPING | LOW | Type error | Wrong TypeScript/Python type, missing generics |');
    w('');
    w('### REFACTOR Sub-Types');
    w('');
    w('| Sub-Type | Risk Default | Description | Verification |');
    w('|----------|-------------|-------------|-------------|');
    w('| REFACTOR:EXTRACT | MEDIUM | Extract function/module/service | Behavior identical, same I/O |');
    w('| REFACTOR:RENAME | HIGH (public) | Rename symbol, file, route | Old name aliased, callers updated |');
    w('| REFACTOR:REORGANIZE | MEDIUM | Move files, restructure dirs | Import paths updated, no runtime change |');
    w('| REFACTOR:SIMPLIFY | LOW | Remove dead code, reduce nesting | Check: nothing depends on removed code |');
    w('| REFACTOR:PATTERN | MEDIUM | Apply design pattern | Behavior identical, structure improved |');
    w('| REFACTOR:TYPE | LOW | Add types, improve generics | Type check passes, no runtime change |');
    w('| REFACTOR:PERF | MEDIUM | Algorithm, data structure change | Faster, same results, tests pass |');
    w('| REFACTOR:API | MEDIUM | Internal API improvement | Interface changes, implementers updated |');
    w('| REFACTOR:CONTRACT | HIGH | Change internal contract | Multiple consumers affected |');
    w('');
    w('### SCHEMA Sub-Types');
    w('');
    w('| Sub-Type | Risk Default | Breaking | Description |');
    w('|----------|-------------|----------|-------------|');
    w('| SCHEMA:DB_ADD | MEDIUM | No | Add nullable column, add table |');
    w('| SCHEMA:DB_REMOVE | CRITICAL | Yes | Drop column, drop table |');
    w('| SCHEMA:DB_RENAME | CRITICAL | Yes | Rename column, rename table |');
    w('| SCHEMA:DB_TYPE | CRITICAL | Yes | Change column type (lossy or not) |');
    w('| SCHEMA:DB_INDEX | MEDIUM | No | Add/drop index |');
    w('| SCHEMA:DB_CONSTRAINT | HIGH | Conditional | Add/drop FK, unique, check |');
    w('| SCHEMA:EVENT_ADD | LOW | No | Add event field (additive) |');
    w('| SCHEMA:EVENT_REMOVE | CRITICAL | Yes | Remove event field |');
    w('| SCHEMA:EVENT_RENAME | CRITICAL | Yes | Rename event field |');
    w('| SCHEMA:MODEL | HIGH | Conditional | Change ORM model, entity |');
    w('| SCHEMA:CONFIG | HIGH | Conditional | Change config schema |');
    w('| SCHEMA:PROTO | HIGH/CRITICAL | Yes (wire) | Change protobuf/thrift/avro |');
    w('| SCHEMA:OPENAPI | HIGH | Conditional | Change OpenAPI spec |');
    w('| SCHEMA:GRAPHQL | HIGH | Conditional | Change GraphQL schema |');
    w('| SCHEMA:CACHE | MEDIUM | No | Change cache key structure |');
    w('');
    w('### CONTRACT Sub-Types');
    w('');
    w('| Sub-Type | Risk Default | Breaking | Description |');
    w('|----------|-------------|----------|-------------|');
    w('| CONTRACT:ROUTE_ADD | LOW | No | Add new endpoint |');
    w('| CONTRACT:ROUTE_REMOVE | CRITICAL | Yes | Remove existing endpoint |');
    w('| CONTRACT:ROUTE_CHANGE | CRITICAL | Yes | Change path, method, auth |');
    w('| CONTRACT:PARAM_ADD_REQ | CRITICAL | Yes | Add required parameter |');
    w('| CONTRACT:PARAM_ADD_OPT | LOW | No | Add optional parameter |');
    w('| CONTRACT:PARAM_REMOVE | CRITICAL | Yes | Remove parameter |');
    w('| CONTRACT:PARAM_RENAME | CRITICAL | Yes | Rename parameter |');
    w('| CONTRACT:PARAM_TYPE | CRITICAL | Yes | Change parameter type |');
    w('| CONTRACT:RESPONSE_ADD | LOW | No | Add response field |');
    w('| CONTRACT:RESPONSE_REMOVE | CRITICAL | Yes | Remove response field |');
    w('| CONTRACT:RESPONSE_RENAME | CRITICAL | Yes | Rename response field |');
    w('| CONTRACT:RESPONSE_TYPE | HIGH | Yes | Change response field type |');
    w('| CONTRACT:STATUS_CODE | HIGH | Yes | Change HTTP status codes |');
    w('| CONTRACT:HEADER | HIGH | Conditional | Add/remove/rename header |');
    w('| CONTRACT:FUNCTION_SIG | HIGH | Yes | Change function signature |');
    w('| CONTRACT:EXPORT | HIGH | Yes | Remove/rename public export |');
    w('| CONTRACT:INTERFACE | HIGH | Yes | Change interface/type |');
    w('| CONTRACT:WEBHOOK | HIGH | Yes | Change webhook payload/schema |');
    w('| CONTRACT:AUTH | CRITICAL | Yes | Change auth method/scope |');
    w('| CONTRACT:RATE_LIMIT | MEDIUM | No | Change rate limit policy |');
    w('| CONTRACT:ERROR | HIGH | Yes | Change error format/codes |');
    w('');
    w('### CONFIG Sub-Types');
    w('');
    w('| Sub-Type | Risk Default | Breaking | Description |');
    w('|----------|-------------|----------|-------------|');
    w('| CONFIG:ENV_ADD | HIGH | Yes (required) | Add required env var |');
    w('| CONFIG:ENV_ADD_OPT | MEDIUM | No | Add optional env var |');
    w('| CONFIG:ENV_REMOVE | HIGH | Yes | Remove env var |');
    w('| CONFIG:ENV_RENAME | CRITICAL | Yes | Rename env var |');
    w('| CONFIG:ENV_VALUE | HIGH | Conditional | Change env var value |');
    w('| CONFIG:ENV_DEFAULT | MEDIUM | Conditional | Change default value |');
    w('| CONFIG:FLAG | MEDIUM | No | Add/remove/change feature flag |');
    w('| CONFIG:SECRET | CRITICAL | Yes | Add/remove/rotate secret |');
    w('| CONFIG:TIMEOUT | MEDIUM | Conditional | Change timeout value |');
    w('| CONFIG:LIMIT | MEDIUM | Conditional | Change rate/capacity limit |');
    w('| CONFIG:RETRY | MEDIUM | No | Change retry policy |');
    w('| CONFIG:REGION | HIGH | Yes | Change deployment region |');
    w('| CONFIG:LOG_LEVEL | LOW | No | Change log level |');
    w('| CONFIG:FEATURE | MEDIUM | Conditional | Enable/disable feature |');
    w('');
    w('### INFRA Sub-Types');
    w('');
    w('| Sub-Type | Risk Default | Description |');
    w('|----------|-------------|-------------|');
    w('| INFRA:DOCKER | HIGH | Dockerfile, compose, registry, build args |');
    w('| INFRA:K8S | HIGH | Manifests, Helm charts, operators, CRDs |');
    w('| INFRA:TERRAFORM | HIGH | Cloud resources, modules, state, backends |');
    w('| INFRA:NETWORK | CRITICAL | Firewall, ingress, DNS, TLS, VPC, subnet |');
    w('| INFRA:SCALING | MEDIUM | Replicas, HPA, VPA, autoscaling policies |');
    w('| INFRA:STORAGE | HIGH | Volumes, PVCs, buckets, storage classes |');
    w('| INFRA:IAM | CRITICAL | Roles, policies, service accounts, trust |');
    w('| INFRA:MONITORING | MEDIUM | Alerts, dashboards, logging, metrics |');
    w('| INFRA:DEPENDENCY | HIGH | Add/remove external service, integration |');
    w('| INFRA:CI | MEDIUM | CI pipeline, build steps, test config |');
    w('| INFRA:CD | HIGH | Deploy pipeline, rollout strategy, approval |');
    w('| INFRA:BACKUP | HIGH | Backup config, retention, restore testing |');
    w('| INFRA:COMPUTE | HIGH | Instance type, capacity, spot vs on-demand |');
    w('| INFRA:REGION | CRITICAL | Multi-region, failover, replication |');
    w('| INFRA:COST | MEDIUM | Resource sizing, reserved instances, savings |');
    w('');
    w('### EXPERIMENT Sub-Types');
    w('');
    w('| Sub-Type | Risk Default | Description | Max Duration |');
    w('|----------|-------------|-------------|-------------|');
    w('| EXPERIMENT:SPIKE | LOW | Research spike | 1 week |');
    w('| EXPERIMENT:POC | LOW | Proof of concept | 2 weeks |');
    w('| EXPERIMENT:PROTOTYPE | LOW | Functional prototype | 1 month |');
    w('| EXPERIMENT:EVAL | LOW | Technology evaluation | 1 week |');
    w('| EXPERIMENT:BENCHMARK | LOW | Performance benchmark | 3 days |');
    w('| EXPERIMENT:A_B_TEST | MEDIUM | A/B test in production | 1 month |');
    w('| EXPERIMENT:FEASIBILITY | LOW | Feasibility study | 1 week |');
    w('');
    w('### INCIDENT Sub-Types');
    w('');
    w('All INCIDENT sub-types default to CRITICAL risk: INCIDENT:OUTAGE, INCIDENT:DATA_LOSS, INCIDENT:SECURITY, INCIDENT:DEGRADED, INCIDENT:ROLLBACK, INCIDENT:MITIGATION, INCIDENT:PERFORMANCE, INCIDENT:COMPLIANCE, INCIDENT:DEPLOYMENT_FAILURE, INCIDENT:CAPACITY.');
    w('');
    w('### ANALYSIS Sub-Types');
    w('');
    w('| Sub-Type | Risk | Description | Tracking |');
    w('|----------|------|-------------|----------|');
    w('| ANALYSIS:CODE | INFO | Explain code, review | No |');
    w('| ANALYSIS:ARCHITECTURE | INFO | Architecture review | No |');
    w('| ANALYSIS:ERROR | INFO | Error analysis | No |');
    w('| ANALYSIS:DIFF | INFO | Diff review | No |');
    w('| ANALYSIS:DEPLOY | INFO | Deploy review | No |');
    w('| ANALYSIS:SECURITY | INFO | Security review | No |');
    w('| ANALYSIS:PERFORMANCE | INFO | Performance analysis | No |');
    w('| ANALYSIS:DEPENDENCY | INFO | Dependency analysis | No |');
    w('| ANALYSIS:COST | INFO | Cost analysis | No |');
    w('');
    w('### Ambiguity Resolution Rules');
    w('');
    w('When a change spans multiple categories, use these resolution rules:');
    w('');
    w('| Ambiguity | Resolution | Rationale |');
    w('|-----------|-----------|----------|');
    w('| FIX vs FEATURE | FIX | Conservative — treat unknown behavior changes as bugs |');
    w('| REFACTOR vs FIX | FIX | Behavior may have changed — verify identical output |');
    w('| CONFIG vs INFRA | INFRA | Higher blast radius — env config affects infra |');
    w('| DOCS vs CONTRACT | CONTRACT | Docs describing a contract = contract change |');
    w('| EXPERIMENT touching production | FEATURE or FIX | Experiment safety stops at production |');
    w('| SCHEMA vs CONFIG | SCHEMA | Schema changes data structure |');
    w('| FEATURE vs REFACTOR | FEATURE | Adding behavior vs preserving it |');
    w('| PLAN vs ANALYSIS | PLAN | Produces artifacts vs not |');
    w('');
    w('### Re-Classification Protocol');
    w('');
    w('During execution, if the actual change deviates from the initial classification:');
    w('1. Stop execution at the current point');
    w('2. Re-classify the change with the correct WorkType and sub-type');
    w('3. Flag in ledger: "Re-classified from [old] to [new]: [reason]"');
    w('4. Re-run risk assessment from S2 with the new classification');
    w('5. If re-classification increases risk, checkpoint before proceeding');
    w('6. If re-classification changes test requirements, update test plan');
    w('');
    w('### Composite Classification');
    w('');
    w('When a user request contains multiple distinct changes, classify each change independently and track as separate ledger entries. The overall session WorkType is the highest-risk type present, but each file write gets its own classification.');
    w('');
    w('Example: "Add a rate limiter to the payment API and fix the timeout error in checkout"');
    w('- Change A: Rate limiter on payment API → FEATURE:PLANNED, MEDIUM');
    w('- Change B: Fix timeout in checkout → FIX:PLANNED, HIGH');
    w('- Composite risk: HIGH (max of both)');
    w('- Ledger: two separate entries');
    w('- Cross-contamination check: do they share code paths? If yes, add to scope coordination.');
    w('');
    w('### WorkType Cross-Check Table');
    w('');
    w('| Primary | Mistaken For | Key Differentiator |');
    w('|---------|-------------|-------------------|');
    w('| FEATURE | REFACTOR | FEATURE adds behavior; REFACTOR preserves it |');
    w('| FIX | REFACTOR | FIX changes behavior; REFACTOR preserves it |');
    w('| SCHEMA | CONFIG | SCHEMA changes data structure; CONFIG changes runtime |');
    w('| CONTRACT | FEATURE | CONTRACT changes boundaries; FEATURE adds internals |');
    w('| CONFIG | INFRA | CONFIG is app-level; INFRA is environment-level |');
    w('| EXPERIMENT | FEATURE | EXPERIMENT has expiry; FEATURE is permanent |');
    w('| ANALYSIS | FIX | ANALYSIS does not modify code |');
    w('| PLAN | ANALYSIS | PLAN produces artifacts |');
    w('| DOCS | CONTRACT | DOCS are descriptive; CONTRACT is prescriptive |');
    w('| INFRA | CONFIG | INFRA provisions resources; CONFIG configures them |');
    w('');
    w('### Classification Edge Cases');
    w('');
    w('**Generated code:** When generating starter code or scaffolds, classify as FEATURE or EXPERIMENT depending on intent. Production scaffold → FEATURE. Disposable prototype → EXPERIMENT. Generated tests → always FEATURE (test code is production code).');
    w('');
    w('**Configuration drift:** When fixing config that drifted from expected values, classify as FIX:CONFIG, not FEATURE. The intent is restoration, not innovation.');
    w('');
    w('**Formatting only:** When applying formatter or linter auto-fix, classify as REFACTOR:SIMPLIFY with LOW risk. But verify: if formatting changes touch runtime-affecting whitespace (Python, YAML), reclassify as MEDIUM.');
    w('');
    w('**Dependency updates:** Patch version bump → REFACTOR:LOW. Minor version → FIX:DEPENDENCY, MEDIUM. Major version → FIX:DEPENDENCY, HIGH. Security advisory → FIX:SECURITY, CRITICAL.');
    w('');
    w('**Rollback:** Classify as INCIDENT:ROLLBACK, CRITICAL. Rollback is a production emergency even if it resolves cleanly. The risk is in the need for rollback, not the rollback operation itself.');
    w('');
    w('**Read-only investigation:** Multiple file reads without any write → always ANALYSIS:CODE. If the investigation leads to a write recommendation, that recommendation is ANALYSIS, and the subsequent write is its own classified action.');
    w('');
    w('**Documentation update:** Updating README as part of a feature → FEATURE (part of the feature). Updating README as a standalone task → DOCS. Updating API docs to match a contract → DOCS, but verify the contract is correct first.');
    w('');
    w('**Test-only changes:** Adding tests for existing functionality → FEATURE:PLANNED with MEDIUM risk (tests change CI behavior). Modifying tests to match new behavior → part of the primary classification. Removing flaky tests → FIX:FLAKE.');
    w('');
    w('**Merge conflict resolution:** Classify as REFACTOR or FIX depending on what the conflict is about. Content conflict → REFACTOR. Logic conflict → FIX. Always verify resolution is correct by running affected tests.');
    w('');
    w('**Code review feedback:** Implementing review feedback on an in-progress change → same classification as the original change. Implementing feedback from a separate PR → new FEATURE or FIX.');
    w('');
    w('**Hotfix vs regular fix:** Hotfix to production → FIX with urgency escalation. Regular fix in development → standard FIX classification. Hotfix carries HIGH risk by default due to reduced testing window.');
    w('');
    w('### Classification Confidence');
    w('');
    w('| Confidence Level | Meaning | Action |');
    w('|-----------------|---------|--------|');
    w('| CERTAIN | Clear intent, explicit request | Proceed normally |');
    w('| LIKELY | Strong signal, some ambiguity | Classify with note |');
    w('| UNCERTAIN | Weak signal, multiple interpretations | Ask one clarifying question |');
    w('| CONTRADICTED | Conflicting signals | Stop, resolve contradiction |');
    w('');
    w('When confidence is UNCERTAIN, emit exactly one question with pre-computed options. Never ask more than one question — if additional clarification is needed, wait for the answer to the first question.');
    w('');
    w('### WorkType-Specific Quality Gates');
    w('');
    w('| WorkType | Required Gates |');
    w('|----------|----------------|');
    w('| FEATURE | Unit tests, contract tests, type check, lint |');
    w('| FIX | Test for the fixed behavior, regression test |');
    w('| REFACTOR | Same tests pass before and after, no new warnings |');
    w('| SCHEMA | Migration rollback, data integrity validation |');
    w('| CONTRACT | Contract tests, consumer verification |');
    w('| CONFIG | Dry-run validation, rollback plan |');
    w('| INFRA | Plan output review, state lock check |');
    w('| INCIDENT | Root cause analysis, monitoring gap review |');
    w('| EXPERIMENT | Expiry date, cleanup plan |');
    w('| DOCS | Technical accuracy review |');
    w('| PLAN | Stakeholder review, ADR format |');
    w('| ANALYSIS | None (no code changes) |');
    w('');
    w('Load references/change-taxonomy.md for complex or ambiguous multi-type changes with full dimension analysis including breadth, reversibility, urgency, detectability, and blast radius.');
    w('');
  }
};
