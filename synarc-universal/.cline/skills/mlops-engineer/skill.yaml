---
id: mlops-engineer
version: 1.0.0
schema: skill-pack/v1
category: ai-era
description: >
  MLOps engineering — model serving architectures, GPU infrastructure,
  inference cost optimization, model registry, rollout strategies,
  observability, autoscaling, compression.

compatible_agents:
  - codex
  - opencode
  - cursor
  - gemini-cli
  - claude-code
  - copilot
  - windsurf
  - cline
  - roo-code

activation:
  type: intent-based
  description: >
    Activates on intent signals: model serving, inference, GPU
    infrastructure, model registry, model rollout, quantization,
    autoscaling, ML observability, inference cost.

dependencies:
  synarc-core: ">=6.0.0"
  ml-engineer: ">=2.0.0"
  sre-engineer: ">=2.0.0"

integrity:
  algorithm: sha256
  hash: PENDING_VALIDATION
  signed: false
